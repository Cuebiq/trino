package io.trino.spi.security.SystemAccessControl

principals = data.rules.principals {  data.rules.principals } else = []

checkCanSetUser{
    count(principals_rules)==0
}else = false {
    input.principal == null
}else = allow {
	regex.match(principals_rules[i].principal,input.principal.name)
    allow = object.get(principals_rules[i],"allow",false)
}else = allow {
    allow = object.get(principals_rules[i],"allow",false)
}



